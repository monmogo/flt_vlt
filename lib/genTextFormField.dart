import 'package:flutter/material.dart';

class getTextFormField extends StatelessWidget {
  TextEditingController? controller;
  String? hintName;
  IconData? iconData;
  bool? is0bsureText;

  getTextFormField(
      {this.controller, this.hintName, this.iconData, this.is0bsureText=false});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
